		================================
		RedirFS - Redirecting FileSystem
			      TODO 
		================================

* allow redirect all operations in VFS objects (in progress with Slava)

Slava
* add a fallback for rfs_file_find and rfs_inode_find to support
  third party hooks, e.g. add a hash table
